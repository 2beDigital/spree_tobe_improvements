<%
   Encoding.default_external = "UTF-8"
   headers = [Spree.t(:login),Spree.t(:updated_at)]
%>
<%= CSV.generate_line headers %><% @collection.each do |user| %><%= CSV.generate_line([user.login,l(user.updated_at.to_date)]) %><% end %>