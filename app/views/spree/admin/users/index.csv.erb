<%
   Encoding.default_external = "UTF-8"
   headers = [Spree.t(:name), Spree.t(:email)]
%>
<%= CSV.generate_line headers %><% @collection.each do |user| %><%= CSV.generate_line([user.login, user.email]) %><% end %>